import os

DATA_CONFIG = {
    "DIR": os.path.dirname(os.path.abspath(__file__)) + '/data/',
    "DOMAINS": (
        "calendar",
        "basketball",
        "blocks",
        "housing",
        "publications",
        "recipes",
        "restaurants",
        "socialnetwork",
    ),
}
